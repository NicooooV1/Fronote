# Block access to internal API directories — only endpoints/ is web-accessible
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(Auth|Core|Database|Services|Events|Jobs|Providers|Legacy|Security|Middleware|Admin|Contracts|UI|[Ll]ogs)(/|$) - [F,L]
</IfModule>
